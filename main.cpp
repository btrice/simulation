#include<iostream>
#include "Tests.h"
int main()
{
	/* if(testMobile1() )
	 	std::cout<<" TESTS VALIDE testMobile 1 \n ";
	 else
		std::cout<<" TESTS NON VALIDE testMobile_1\n ";
	if(testSimulation1() )
	 	std::cout<<" TESTS VALIDE testSimulation_1 \n ";
	 else
		std::cout<<" TESTS NON VALIDE testSimulation_1\n ";
	if(testSimulation2())
	 	std::cout<<" TESTS VALIDE  testSimulation_2\n ";
	 else
		std::cout<<" TESTS NON VALIDE testSimulation-2\n ";
	if(testMobile2())
	 	std::cout<<" TESTS VALIDE  testMobile2()\n ";
	 else
		std::cout<<" TESTS NON VALIDE testMobile2()\n ";

	if(testVecteur3D())
	 	std::cout<<" TESTS VALIDE  testVecteur3D()\n ";
	 else
		std::cout<<" TESTS NON VALIDE testVecteur3D() \n ";
	if(testTerre())
	 	std::cout<<" TESTS VALIDE  testTerre()\n ";
	 else
		std::cout<<" TESTS NON VALIDE testTerre()\n ";*/
	if(testSatellite1())
	 	std::cout<<" TESTS VALIDE  testTerre()\n ";
	 else
		std::cout<<" TESTS NON VALIDE testTerre()\n ";
	if(testIntegrateur2())
	 	std::cout<<" TESTS VALIDE  testIntegrateur2()\n ";
	 else
		std::cout<<" TESTS NON VALIDE testIntegrateur2()\n ";
return 0;
}
